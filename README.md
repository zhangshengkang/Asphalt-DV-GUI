# SGA–XGBoost GUI for DV Prediction

This repository provides a graphical user interface (GUI) implemented
using **Gradio** for the SGA-optimized XGBoost model described in the manuscript.

## Overview
The GUI allows users to input asphalt material properties and environmental
parameters to obtain the predicted damage variable (DV).

## Usage

1. Install required dependencies:
   ```bash
   pip install gradio xgboost pandas
